#include "../inc.hpp"

class LC_ContiguousArray {
public:
    int findMaxLength(vector<int>& nums) {
        
    }
};

TEST_CASE("LC: Contiguous Array") {
    auto s = make_shared<LC_ContiguousArray>();
}